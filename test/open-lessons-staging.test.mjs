import assert from 'node:assert/strict';import test from 'node:test';import{cleanupSql,LESSON_IDS,run,seedSql}from'../scripts/seed-open-lessons-staging.mjs';
test('creates a separate bilingual offline BRASA Open staging catalog',()=>{const sql=seedSql(new Date('2026-09-11T12:00:00Z'));assert.equal(LESSON_IDS.length,10);assert.match(sql,/brasa-open-staging/);assert.match(sql,/\["en","es"\]/);assert.equal((sql.match(/1,'published'/g)||[]).length,10);assert.doesNotMatch(sql,/contract-test-school/)});
test('is staging-only and idempotent',()=>{const calls=[];const result=run('apply',sql=>calls.push(sql),new Date('2026-09-11T12:00:00Z'));assert.equal(result.schoolId,'brasa-open-staging');assert.equal((calls[0].match(/ON CONFLICT/g)||[]).length,11);assert.throws(()=>run('production',()=>{}),/staging only/)});
test('cleanup names only the dedicated school and fixed lessons',()=>{const sql=cleanupSql();for(const id of LESSON_IDS)assert.match(sql,new RegExp(id));assert.doesNotMatch(sql,/LIKE|GLOB|status=/)});
